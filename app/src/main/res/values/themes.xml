<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only used for the pre-Compose launch window. Compose owns everything after
      that, so this is deliberately bare: no action bar, transparent system bars
      (enableEdgeToEdge configures them at runtime), and a background that
      matches the light scheme so the cold-start flash isn't jarring.
    -->
    <style name="Theme.PlainlyMusic" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
